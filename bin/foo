#!/bin/zsh 


mypath=${0:a:h}
foo="toto"

total="${mypath}${foo}"
echo $total




#portable way
#abs_path() {
  #perl -MCwd -le '
    #for (@ARGV) {
      #if ($p = Cwd::abs_path $_) {
        #print $p;
      #} else {
        #warn "abs_path: $_: $!\n";
        #$ret = 1;
      #}
    #}
    #exit $ret' "$@"
#}

